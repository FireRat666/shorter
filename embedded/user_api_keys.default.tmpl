<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="color-scheme" content="dark">
    <link rel="stylesheet" type="text/css" href="/shorter.css" integrity="{{.CssSRIHash}}">
    <title>API Key Management</title>
</head>
<body>
    <div class="content">
        <div class="header">
            <img src="/logo.png">
            <h1>API Key Management</h1>
        </div>
        <div class="header-links">
            <a href="/user/" class="button">Dashboard</a>
            <a href="/user/api-keys" class="button button-yellow">API Keys</a>
            <a href="/user/security" class="button">Security</a>
            <a href="/logout" class="button button-red">Logout</a>
        </div>

        {{if .NewKey}}
        <div class="tos">
            <h2>New API Key Generated</h2>
            <p class="info">Please copy this key now. You will not be able to see it again.</p>
            <div class="destination-box">
                <code>{{.NewKey}}</code>
            </div>
        </div>
        {{end}}

        <div class="tos">
            <h2>Your API Keys</h2>
            <form class="search-form" method="GET" action="/user/api-keys">
                <input type="text" name="q" class="inputbox" placeholder="Search by key..." value="{{.SearchQuery}}">
                <input type="submit" value="Search" class="button">
                <a href="/user/api-keys" class="button clear-button">Clear</a>
            </form>

            {{if .APIKeys}}
                <table>
                    <thead>
                        <tr>
                            <th>Key (first 8 characters)</th>
                            <th>Description</th>
                            <th>Created At</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{range .APIKeys}}
                        <tr>
                            <td><code>{{.Token | printf "%.8s"}}...</code></td>
                            <td>{{.Description}}</td>
                            <td>{{.CreatedAt.Format "2006-01-02 15:04 MST"}}</td>
                            <td>
                                <form method="POST" action="/user/api-keys">
                                    <input type="hidden" name="csrf_token" value="{{$.CSRFToken}}">
                                    <input type="hidden" name="action" value="delete">
                                    <input type="hidden" name="token" value="{{.Token}}">
                                    <input type="submit" value="Delete" class="button-red" data-confirm="Are you sure you want to delete this API key? Any applications using it will stop working.">
                                </form>
                            </td>
                        </tr>
                        {{end}}
                    </tbody>
                </table>
            {{else}}
                <p>No API keys found.</p>
            {{end}}
            {{if .APIKeys}}
            <div class="pagination-controls">
                {{if .HasPrev}}
                    <a href="/user/api-keys?q={{.SearchQuery}}&page={{sub .CurrentPage 1}}" class="pagination-button">&laquo; Previous</a>
                {{end}}
                {{if gt .TotalPages 1}}
                    <span>Page {{.CurrentPage}} of {{.TotalPages}}</span>
                {{end}}
                {{if .HasNext}}
                    <a href="/user/api-keys?q={{.SearchQuery}}&page={{add .CurrentPage 1}}" class="pagination-button">Next &raquo;</a>
                {{end}}
            </div>
            {{end}}
            <form class="admin-form" method="POST" action="/user/api-keys">
                <input type="hidden" name="csrf_token" value="{{.CSRFToken}}">
                <input type="hidden" name="action" value="generate">
                <label for="description">Description:</label>
                <input type="text" id="description" name="description" class="inputbox" placeholder="e.g., 'CI/CD Pipeline Key'">
                <input type="submit" value="Generate New API Key" class="admin-submit-button">
            </form>
        </div>
    </div>
    <div class="footer-link">
        <a href="https://github.com/FireRat666/shorter" target="_blank" rel="noopener noreferrer">Source Code on GitHub</a>
    </div>
</body>
</html>